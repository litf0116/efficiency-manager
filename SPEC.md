# 仓储人效管理系统 - 产品规格书

## 1. 项目概述

### 1.1 项目背景
- **用户**：朋友公司的仓储物流部门（50-200人规模）
- **现状**：使用Excel管理人效数据，存在数据分散、更新不及时、缺乏可视化问题
- **目标**：数字化人效管理，提升数据利用率和决策效率

### 1.2 核心价值
- **简单实用**：继承Excel的数据结构，降低迁移成本
- **快速见效**：2-4周上线核心功能
- **渐进演化**：从基础看板到智能分析

### 1.3 技术选型

| 阶段 | 技术方案 | 理由 |
|------|----------|------|
| **MVP v1** | 单HTML文件 + localStorage | 零部署，立即可用 |
| **v2** | Tauri桌面应用 | 跨平台，原生体验 |
| **v3** | Node.js + SQLite | 多用户，数据安全 |

---

## 2. 功能规划

### 2.1 MVP功能清单（v1）

| 功能 | 描述 | 优先级 | 复杂度 |
|------|------|--------|--------|
| **数据导入** | Excel文件导入，自动解析人效数据 | P0 | 低 |
| **数据管理** | 仓库、小组、客户增删改查 | P0 | 低 |
| **人效看板** | 各小组人效月度趋势图 | P0 | 低 |
| **排名视图** | 小组人效排名柱状图 | P0 | 低 |
| **目标设定** | 各小组人效目标值设置 | P1 | 低 |
| **数据导出** | Excel导出月度报表 | P1 | 低 |
| **异常预警** | 人效低于阈值高亮显示 | P2 | 中 |

### 2.2 后续版本功能（v2+）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 扫码报工 | 任务开始/结束扫码记录 | P1 |
| 实时看板 | 当日产出/效率大屏 | P2 |
| 移动端适配 | 手机查看绩效 | P2 |
| 预测分析 | 人力需求预测 | P3 |

---

## 3. 数据模型

### 3.1 核心实体

```
┌─────────────┐       ┌─────────────┐
│  Warehouse  │       │    Team     │
├─────────────┤       ├─────────────┤
│ id          │──┐    │ id          │
│ name        │  │    │ warehouse_id│←─┐
│ location    │  └───→│ name        │  │
└─────────────┘       │ floor       │  │
                      │ function    │  │
                      └─────────────┘  │
                              ▲        │
                              │        │
                    ┌─────────┴────────┘
                    │  EfficiencyLog
                    ├─────────────────┐
                    │ id              │
                    │ team_id        │←─┐
                    │ year           │  │
                    │ month          │  │
                    │ efficiency     │  │
                    │ total_output   │  │
                    │ std_output     │  │
                    └─────────────────┘

┌─────────────┐       ┌─────────────┐
│  Customer   │       │   StdRate   │
├─────────────┤       ├─────────────┤
│ id          │       │ id          │
│ warehouse_id│──────→│ operation   │
│ name        │       │ unit        │
│ category    │       │ std_rate    │
└─────────────┘       └─────────────┘
```

### 3.2 localStorage 数据键

| 键名 | 数据类型 | 说明 |
|------|----------|------|
| `warehouses` | Array | 仓库列表 |
| `teams` | Array | 小组列表 |
| `customers` | Array | 客户列表 |
| `stdRates` | Array | 标准人效列表 |
| `efficiencyLogs` | Array | 人效日志 |
| `settings` | Object | 系统设置(含目标阈值) |

---

## 4. UI设计

### 4.1 页面结构

```
┌──────────────────────────────────────────────────────────┐
│  仓储人效管理系统                              [设置]    │
├──────────────────────────────────────────────────────────┤
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐  │
│  │ 数据导入 │ │ 看板   │ │ 数据管理│ │ 目标   │ │ 报表   │  │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘  │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                    [主内容区]                             │
│                                                          │
│  - 数据导入：拖拽Excel文件                                │
│  - 看看板：图表展示                                      │
│  - 数据管理：表格CRUD                                    │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 4.2 页面说明

#### 4.2.1 数据导入页
- 拖拽区域接收Excel文件
- 自动识别表结构
- 预览解析结果
- 确认导入/取消

#### 4.2.2 人效看板页
- **顶部卡片**：总小组数、平均人效、达标率、异常数
- **左图表**：各小组9个月人效趋势折线图
- **右图表**：最新月份人效排名柱状图
- **底部表格**：详细数据明细

#### 4.2.3 数据管理页
- Tab切换：仓库 | 小组 | 客户 | 标准人效
- 表格展示 + 新增/编辑/删除按钮
- 搜索/筛选功能

#### 4.2.4 目标设定页
- 小组列表 + 目标人效输入框
- 保存后看板自动标注达标/未达标

#### 4.2.5 报表导出页
- 选择月份范围
- 勾选导出内容
- 生成Excel下载

---

## 5. 验收标准

### 5.1 功能验收

| 功能 | 验收条件 |
|------|----------|
| 数据导入 | 可导入现有Excel格式，正确解析并存储 |
| 数据管理 | 可新增/编辑/删除仓库、小组、客户、标准人效 |
| 人效看板 | 显示趋势图、排名图，数据与导入一致 |
| 目标设定 | 可设置目标值，看板高亮未达标 |
| 数据导出 | 导出Excel包含所有必需字段 |

### 5.2 体验验收

| 指标 | 标准 |
|------|------|
| 首屏加载 | < 2秒 |
| 图表渲染 | < 500ms |
| 数据导入 | < 5秒（1000行） |
| 操作反馈 | 所有按钮有点击响应 |

---

## 6. 技术实现

### 6.1 技术栈

| 层次 | 技术 | 版本 |
|------|------|------|
| 页面框架 | 原生HTML/CSS/JS | - |
| 图表库 | Chart.js | 4.x |
| Excel解析 | SheetJS (xlsx.js) | 0.20.x |
| 图标 | Lucide Icons | - |
| 存储 | localStorage | - |

### 6.2 文件结构

```
efficiency-manager/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式文件
├── js/
│   ├── app.js          # 主应用逻辑
│   ├── storage.js      # localStorage封装
│   ├── parser.js       # Excel解析器
│   ├── charts.js      # 图表渲染
│   └── utils.js       # 工具函数
├── data/
│   └── sample.xlsx     # 示例数据
├── SPEC.md            # 本文档
└── README.md          # 使用说明
```

### 6.3 关键实现

#### Excel解析
```javascript
// 使用SheetJS解析Excel
const workbook = XLSX.read(data, { type: 'array' });
const sheet = workbook.Sheets[workbook.SheetNames[0]];
// 转换为JSON
const jsonData = XLSX.utils.sheet_to_json(sheet);
```

#### 数据存储
```javascript
// localStorage封装
const Storage = {
  get: (key) => JSON.parse(localStorage.getItem(key) || '[]'),
  set: (key, value) => localStorage.setItem(key, JSON.stringify(value)),
  clear: () => localStorage.clear()
}
```

---

## 7. 开发计划

### Phase 1: 基础框架（1-2天）
- [ ] 创建项目结构
- [ ] 实现localStorage封装
- [ ] 实现Excel解析器
- [ ] 基础UI布局

### Phase 2: 核心功能（2-3天）
- [ ] 数据导入功能
- [ ] 数据管理CRUD
- [ ] 人效看板图表
- [ ] 目标设定功能

### Phase 3: 完善功能（1-2天）
- [ ] 数据导出
- [ ] 异常预警
- [ ] 样式优化
- [ ] 测试验证

---

## 8. 后续展望

### v2: 桌面应用
- 使用 Tauri 打包为桌面应用
- 使用 SQLite 替代 localStorage
- 支持多用户

### v3: 网络版
- Node.js + Express 后端
- PostgreSQL 数据库
- 用户权限管理
- API开放

---

*文档版本: v1.0*
*创建日期: 2025-04-25*
